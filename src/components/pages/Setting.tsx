import { memo } from "react";
import HeaderLayout from "../templates/HeaderLayout";

const Setting = memo(() => {
  return (
    <>
      <HeaderLayout>
        <p>Settingページ</p>
      </HeaderLayout>
    </>
  );
});

export default Setting;
