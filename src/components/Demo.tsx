import { Button, HStack } from "@chakra-ui/react";

export const Demo = () => {
  return (
    <HStack>
      <Button bgColor="gray.600">Click me</Button>
      <Button bgColor="red.700">Click me</Button>
    </HStack>
  );
};
